{
  "name": "meetnearme-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:tw": "npx npx tailwindcss -o ./static/styles.css --minify",
    "build:go": "go build -o ./dist/main .",
    "build": "npm run build:tw && npm run build:go",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "daisyui": "^4.7.2",
    "tailwindcss": "^3.4.1"
  }
}
